# SEO Cannibalization and Internal Linking Report — `<domain>`

Date: `<YYYY-MM-DD>`  
Country: `<country>`  
Language: `<language>`

## Executive summary

Summarize confirmed multi-URL keyword clusters, selected primary pages, highest-priority consolidation or differentiation actions, and internal-link opportunities. Distinguish confirmed evidence from hypotheses.

## Scope

- Domain, country, language, and requested URLs
- Report root and analysis date
- Relevant Pages, Ranked Keywords, and Page Intersection limits and returned coverage
- Parsed page count and selection method
- Explicit priority-page/site-wide graph limitation
- `Total cost: x,xx USD`

## Candidate-page coverage

List included and excluded URLs, organic ETV, keyword count, ranking distribution, selection reason, and missing fields.

## Multi-URL keyword matrix

For every detected keyword or coherent same-intent cluster, report search volume, difficulty, intent, each ranking URL and position, ETV where available, evidence source, and confidence. Do not collapse distinct intents.

## Primary and competing pages

For each cluster, identify the selected primary page and competing pages. Explain intent fit, current visibility, topical completeness, technical/canonical evidence, internal-link support, business priority when supplied, uncertainty, and why multiple URLs are harmful or acceptable.

## Action plan

Use one row per implementation decision:

| Priority | Cluster | Decision | Affected URL | Target URL | Exact change | Evidence | Risk | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |

Cover **merge**, **redirect**, **canonicalize**, **differentiate**, or **retain** as evidence warrants. For merges, specify content to preserve and the redirect sequence. For differentiation, assign distinct keyword and intent ownership plus concrete title, heading, copy, and anchor changes.

## Sampled internal-link map

| Source URL | Target URL | Recommended anchor | Placement/context | Existing-link status | Rationale |
| --- | --- | --- | --- | --- | --- |

Separate new links from retained or revised links. Mark every source not parsed as unverified; avoid repetitive exact-match anchors.

## Page briefs

Provide a brief for every priority target and competing page: role, owned keyword cluster, content changes, metadata/heading changes, consolidation handling, links to add/remove/repoint, and post-change checks.

## Measurement and validation

Define pre-change baselines and checks after implementation: indexability, canonical/redirect behavior, internal-link destinations, ranking URL stability, positions, and ETV. Give an observation window without promising results.

## Methodology and limitations

Explain URL normalization, same-intent grouping, primary-page criteria, confidence, null handling, sampling, weekly Labs freshness, and why multiple URLs ranking does not automatically establish cannibalization. State that Instant Pages and Content Parsing do not replace a task-based site crawl and the report does not guarantee a complete internal-link graph.

## DataForSEO call log

| Endpoint | Purpose | Requested limit/pages | Returned coverage | Status | Cost (USD) | Notes |
| --- | --- | --- | --- | --- | --- | --- |

Sum unrounded costs, format the displayed total with a decimal comma and two digits, and identify missing cost fields.
