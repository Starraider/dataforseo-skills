# SERP Optimization Report: `<domain>`

Date: `<YYYY-MM-DD>`

## Executive summary

- Highest-priority opportunity:
- Strongest attainable feature:
- Main page-format gap:
- Important external dependency:
- Coverage caveat:

## Scope and cost

- Domain:
- Country / exact location:
- Language:
- Device:
- Discovery coverage: top-20 organic positions, limit 200
- Approved live shortlist:
- Live capture timestamps:
- Parsed target pages / winning pages:
- Total cost: `x,xx USD`

State every user input, default, approval, incomplete cost, failed call, sampling limit, and degraded parsing mode.

## SERP-feature opportunity matrix

| Priority | Score | Keyword | Volume | Intent | Target URL | Target live position | Feature | Feature absolute position | Current owner | Winning page format | Attainability | Primary change |
| --- | ---: | --- | ---: | --- | --- | --- | --- | ---: | --- | --- | --- | --- |

Use `unavailable`, `not exposed`, and `not found within depth 100` instead of invented values. Include Owned and Stale/absent rows, but do not give them opportunity scores.

## Current owners and result composition

For each approved keyword:

### `<keyword>`

- Target organic `rank_group` / `rank_absolute` and URL:
- Live result composition in absolute order:
- Analyzed feature owner or nested owners:
- Owner page format (analyst inference) and evidence:
- Feature prominence:
- Discovery-to-live differences:

## Page comparisons and exact changes

Group recommendations by target URL, then feature.

### `<target URL>` — `<feature>` for `<keyword>`

#### Evidence

- Current target structure:
- Winning page(s) and observed structure:
- Intent and format fit:
- Attainability classification and rationale:

#### Exact implementation

1. **Insertion point:** Name the existing section or structural location.
2. **Heading:** Supply the exact proposed H2/H3.
3. **Answer block:** Specify purpose, form, and evidence-based length or composition. Draft page-specific copy only when supported by source content.
4. **List or table:** Give exact step labels, bullet themes, or table columns when the live winner uses that form.
5. **Imagery:** Specify subject, placement, original-asset requirement, alt/caption purpose, and indexability where relevant.
6. **Video:** Specify placement, topic, thumbnail, transcript/captions, and visible supporting text where relevant.
7. **Structured data:** Name only an eligible type and the visible facts it may represent; otherwise state `none recommended`.
8. **Supporting questions:** List only returned PAA questions and state where they fit.
9. **External dependency:** Identify Business Profile, merchant feed, inventory, review, or platform work that cannot be solved by page edits.

#### Success check

- Re-crawl check:
- Re-run market/device:
- Feature and target-position change to monitor:
- No-guarantee caveat:

## Structured-data and eligibility notes

Separate page changes from external eligibility. Never claim that markup, a content length, or matching the current winner guarantees a feature.

## Methodology

Document normalization, discovery filter, live SERP settings, parse selection, attainability rubric, full priority formula, component values, and analyst inferences.

## Coverage and limitations

Describe the 200-row discovery cap, approved shortlist, one-page live depth, location sensitivity, device specificity, volatile feature ownership, parsing gaps, inaccessible owners, and incomplete metrics.

## DataForSEO call log

| # | Tool | Scope | Status | Returned items | Cost USD | Limitation |
| ---: | --- | --- | --- | ---: | ---: | --- |

## Sources

List the target and parsed winner URLs plus the official DataForSEO endpoint documentation. Do not store raw MCP response payloads containing customer information.
